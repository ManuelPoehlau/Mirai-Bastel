"""Performance-Research für den Viewport-V1-Praxistest (nur Messung, keine Optimierung).

Alle Tools hier arbeiten ausschliesslich per Monkey-Patching auf Instanzen —
es wird bewusst KEINE bestehende Viewport-/Core-Datei geändert (Task-Regel:
erst messen, dann entscheiden).
"""
